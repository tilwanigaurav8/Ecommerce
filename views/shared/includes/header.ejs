<header>
    <div><a href="/">GTcom</a></div>
    <nav>
        <ul>
            <% if(locals.isAuth) {%>
                <li>
                    <button>Logout</button>
                </li>
            <% } %>
        </ul>
    </nav>
</header>
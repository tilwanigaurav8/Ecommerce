<header>
    <div><a href="/">GTcom</a></div>
    <nav>
        <ul>
            <% if(locals.isAuth) {%>
                <li>
                    <form action="/logout" method="POST">
                        <input type="hidden" name="_csrf" value="<%= locals.csrfTokens %>" id="">
                        <button>Logout</button>
                    </form>
                </li>
            <% } %>
        </ul>
    </nav>
</header>